// 基础影院数据配置
// 所有尺寸均来自真实 IMAX GT Laser 影厅（万达华南MALL店）
// 修改此文件即可调整影厅规格，不影响其他模块
export const HALL = {
    // 整体空间（v9：加宽加高以容纳巨型银幕与阶梯看台）
    width: 66,            // 影厅内部宽度（米）—— 超过银幕宽(55.6)，两侧各留余量
    depth: 60,            // 影厅内部纵深（米）
    height: 45,           // 影厅净高（米）—— 高于银幕顶(41.4)与最后排(33)

    // 银幕（v6：尺寸翻倍，增强巨幕震撼感）
    screen: {
        width: 55.6,      // 银幕宽（米）—— 翻倍后超巨型银幕
        height: 40.6,     // 银幕高（米）—— 翻倍后约 24 层楼高
        curvature: 0.06,  // 银幕弧度系数（GT 银幕轻微内凹）
        zOffset: 2.0      // 银幕前移量（距后墙）
    },

    // 阶梯式长条座椅看台（v9：整排长椅 + 45° 阶梯地面）
    // 一排 = 一条与银幕等宽的长椅；一排排向后、按 45° 阶梯叠加
    // 直到最后一排高度达到银幕高度的 4/5（≈32.5m）
    seat: {
        benchWidth: 55.6,   // 每排长椅宽度 = 银幕宽度（米）
        benchDepth: 1.2,    // 长椅座深（米）
        seatHeight: 0.5,    // 座面离台阶面的高度（米）
        backHeight: 0.95,   // 靠背高度（米）
        run: 2.2,           // 每排水平进深（米）
        rise: 2.2,          // 每排抬升高度（米）—— run=rise => 倾斜角 45°
        frontZ: -20,        // 第一排中心 z（距银幕约 8m）
        rows: 16,           // 排数（叠加到 (16-1)×2.2 ≈ 33m，约银幕高 4/5）
        cols: 20,           // 选座时沿长椅横向的虚拟座位列数
        platformMargin: 4,  // 看台地面宽出墙壁的量（两侧各 2m，穿墙防漏洞）
        eyeHeight: 1.6      // 视点离地高度（米）
    }
};
